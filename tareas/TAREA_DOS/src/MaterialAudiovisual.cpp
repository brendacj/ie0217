#include "MaterialAudiovisual.hpp"

MaterialAudiovisual::MaterialAudiovisual(
    string titulo,
    string grupo,
    string material,
    string autor,
    string genero,
    string estado,
    int duracion,
    double precio
) : titulo(titulo),
    grupo(grupo),
    material(material),
    autor(autor),
    genero(genero),
    estado(estado),
    duracion(duracion),
    precio(precio){}